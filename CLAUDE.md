## Agent skills

Shared skills are sourced from [`vivedlearning/vived-skills`](https://github.com/vivedlearning/vived-skills)
as a git submodule at `.github/skills/`. After cloning, run:

    git submodule update --init --recursive

Project-specific skills (if any) live in `.claude/skills/`.
